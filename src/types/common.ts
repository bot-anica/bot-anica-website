export enum ButtonVariant {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    OUTLINE = 'outline',
    GHOST = 'ghost',
}
export enum ButtonSize {
    SM = 'sm',
    MD = 'md',
    LG = 'lg',
    ICON = 'icon',
}
export enum ButtonIconPosition {
    LEFT = 'left',
    RIGHT = 'right',
}
